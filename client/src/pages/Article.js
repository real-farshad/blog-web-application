import Navbar from "../components/Navbar";

export default function Article({ article }) {
    return (
        <>
            <header>
                <Navbar />
            </header>

            <main></main>

            <footer></footer>
        </>
    );
}
